{
  "moduleId": "process",
  "name": "processtask_focus",
  "label": "Work Order Attention Person Table",
  "description": "The work order attention person table is used for related queries and report queries related to workflow scenarios.",
  "fields": [
    {
      "name": "processtask_id",
      "type": "bigint",
      "description": "Work order ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "user_uuid",
      "type": "char(32)",
      "description": "User UUID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "processtask_id",
        "user_uuid"
      ],
      "description": "Primary key index covering the processtask_id, user_uuid fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
