{
  "moduleId": "process",
  "name": "processtask_collection_auth",
  "label": "流程processtask_collection_auth表",
  "description": "流程processtask_collection_auth表，属于流程模块的数据表。 可用于流程相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "collection_id",
      "type": "bigint",
      "description": "collection关联ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_id",
      "type": "char(32)",
      "description": "auth关联ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_type",
      "type": "varchar(50)",
      "description": "流程processtask_collection_auth表的auth_type字段，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "collection_id",
        "auth_id",
        "auth_type"
      ],
      "description": "主键索引，覆盖 collection_id、auth_id、auth_type 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_user_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "auth_id"
      ],
      "description": "普通索引，覆盖 auth_id 字段，适合用于过滤、关联或排序查询。"
    }
  ]
}
