{
  "moduleId": "process",
  "name": "processtask_collection_definition_tag",
  "label": "process_task_collection_definition_tag Table",
  "description": "The process task collection definition tag table, which belongs to the process module, is used for related queries and report queries related to workflows.",
  "fields": [
    {
      "name": "tag_name",
      "type": "varchar(50)",
      "description": "Attribute name, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "tag_value",
      "type": "varchar(255)",
      "description": "Attribute values, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "definition_id",
      "type": "bigint",
      "description": "Define ID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "definition_id",
        "tag_name",
        "tag_value"
      ],
      "description": "Primary key index covering the definition_id, tag_name, tag_value fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
