{
  "moduleId": "process",
  "name": "process_step_task_config",
  "label": "Task Table for Process Steps",
  "description": "Step-by-step task table for workflows, used for related queries and report scenarios.",
  "fields": [
    {
      "name": "process_step_uuid",
      "type": "char(32)",
      "description": "Workflow step UUID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "task_config_id",
      "type": "bigint",
      "description": "Task ID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "process_step_uuid",
        "task_config_id"
      ],
      "description": "Primary key index covering the process_step_uuid, task_config_id fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
