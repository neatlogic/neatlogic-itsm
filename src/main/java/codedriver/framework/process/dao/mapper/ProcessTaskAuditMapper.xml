<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.process.dao.mapper.ProcessTaskAuditMapper">

	<select id="getProcessTaskAuditList" resultType="codedriver.module.process.dto.ProcessTaskStepAuditVo">
		SELECT 
		`processtask_id` as processTaskId,
		`processtask_step_id` as processTaskStepId,
		`user_id` as userId,
		`action_time` as actionTime,
		`action`
		FROM
		  `processtask_step_audit`
		WHERE 1=1
		<if test="processTaskId != null">
	 		and `processtask_id` = #{processTaskId}
	 	</if>
		<if test="userId != null">
	 		and `user_id` = #{userId}
	 	</if>
	 	<if test="action != null">
	 		and `action` = #{action}
	 	</if>
	</select>

	<insert id="insertProcessTaskStepAudit" parameterType="codedriver.module.process.dto.ProcessTaskStepAuditVo">
		<selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
			select LAST_INSERT_ID() as id
		</selectKey>
		INSERT INTO `processtask_step_audit` (
		`processtask_id`,
		`processtask_step_id`,
		`user_id`,
		`action_time`,
		`action`
		)
		VALUES
		(
		#{processTaskId},
		#{processTaskStepId},
		#{userId},
		NOW(3),
		#{action}
		)
	</insert>
</mapper>